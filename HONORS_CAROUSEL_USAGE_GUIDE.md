# 🏆 荣誉榜轮播策略使用指南

## 📋 概述

荣誉榜轮播是一个特殊的展示模式，用于突出显示获得荣誉的员工。当您点击"★荣誉榜★"按钮时，系统会自动筛选出所有有荣誉记录的员工，并按照与核心骨干相同的轮播策略展示他们。

---

## 🎯 核心功能

### 1. 荣誉员工筛选
- **触发方式**：点击页面顶部的"★荣誉榜★"按钮
- **筛选逻辑**：系统自动过滤出所有 `honors` 数组不为空的员工
- **显示效果**：只有获得过荣誉的员工才会在荣誉榜中显示

### 2. 中心排序算法
荣誉榜采用与核心骨干相同的中心排序算法：

```
原始员工列表：[A, B, C, D, E, F, G, H, I, J]
              ↓ 中心排序
排序后列表：  [E, D, F, C, G, B, H, A, I, J]
              ↑ 中心员工（最优先展示）
```

**排序规则**：
- 找到列表的中间位置（E）作为中心
- 从中心向两侧交替扩展
- 排序后的前 5 个员工优先在详情卡片中展示

### 3. 轮播展示模式

#### 批次轮播（自动）
- **时间间隔**：每 5 秒自动切换一次
- **显示方式**：10 个员工为一批，从左到右滑动
- **循环方式**：最后一批后回到第一批

#### 详情轮播（自动）
- **触发条件**：30 秒无用户操作
- **时间间隔**：每 5 秒自动切换一个员工
- **显示内容**：员工详细信息卡片 + 所有荣誉记录

---

## 🚀 使用步骤

### 第一步：准备荣誉数据
在管理后台为员工添加荣誉记录：

1. 登录管理后台
2. 进入"员工管理"
3. 选择要添加荣誉的员工
4. 在"荣誉管理"部分添加荣誉信息
5. 保存更改

**示例荣誉数据**：
```
员工：张三
荣誉：
  - 2024年度优秀管理者
  - 2023年度先进个人
  - 2022年度技术创新奖
```

### 第二步：进入荣誉榜视图
1. 打开大屏展示页面
2. 点击页面顶部的"★荣誉榜★"按钮
3. 系统自动切换到荣誉榜模式

### 第三步：观看轮播展示
- **批次轮播**：自动每 5 秒切换一批员工
- **翻页操作**：点击左下角"←"或右下角"→"按钮手动翻页
- **详情查看**：点击任何员工照片查看详细信息

### 第四步：查看员工荣誉
点击员工照片后，详情卡片会显示：
- 员工基本信息（名字、部门、岗位等）
- 所有荣誉记录（以列表形式显示）
- 工作职责和工作信条

---

## 📊 数据流程

```
管理后台
   ↓
添加员工荣誉记录
   ↓
数据库 honors 表
   ↓
后端 API (employees.get)
   ↓
返回员工 + 荣誉数据
   ↓
前端过滤 (honors.length > 0)
   ↓
荣誉榜视图
   ↓
中心排序 + 轮播展示
```

---

## 🔄 轮播工作流程

### 荣誉榜轮播流程图

```
用户进入大屏
    ↓
点击"★荣誉榜★"
    ↓
系统筛选有荣誉的员工
    ↓
中心排序（优先级排序）
    ↓
显示第一批员工（10人）
    ↓
自动轮播（每5秒切换）
    ↓
30秒无操作
    ↓
启动详情轮播
    ↓
自动显示员工详情卡片
    ↓
每5秒切换一个员工
    ↓
显示该员工的所有荣誉
    ↓
用户点击照片/按钮
    ↓
停止自动轮播，进入手动模式
```

---

## 💡 使用场景

### 场景 1：公司表彰大会
- 在会议现场播放荣誉榜轮播
- 自动循环展示所有获奖员工
- 每个员工详情卡片显示其所有荣誉

### 场景 2：员工风采展示
- 在公司大厅大屏上展示荣誉榜
- 吸引访客关注优秀员工
- 激励其他员工争取荣誉

### 场景 3：年度总结汇报
- 在年度总结会上展示荣誉榜
- 突出表彰优秀员工的成就
- 展示公司的人才培养成果

---

## ⚙️ 配置参数

### 轮播时间配置

| 参数 | 默认值 | 说明 |
|-----|-------|------|
| 批次轮播间隔 | 5 秒 | 每 5 秒切换一批员工 |
| 详情轮播间隔 | 5 秒 | 每 5 秒切换一个员工 |
| 无操作触发时间 | 30 秒 | 30 秒无操作后启动详情轮播 |
| 每批员工数 | 10 人 | 每次显示 10 个员工 |
| 优先展示数 | 5 人 | 中心排序后的前 5 人优先展示 |

### 修改配置方法

在 `Showcase.tsx` 中找到以下常量并修改：

```typescript
// 第 XX 行
const CAROUSEL_INTERVAL = 5000; // 批次轮播间隔（毫秒）
const DETAIL_CAROUSEL_INTERVAL = 5000; // 详情轮播间隔（毫秒）
const INACTIVITY_TIMEOUT = 30000; // 无操作触发时间（毫秒）
const BATCH_SIZE = 10; // 每批员工数
const PRIORITY_COUNT = 5; // 优先展示数
```

---

## 🐛 常见问题

### Q1：荣誉榜显示"暂无员工"
**原因**：数据库中没有员工有荣誉记录

**解决方案**：
1. 进入管理后台
2. 为至少一个员工添加荣誉记录
3. 刷新大屏页面

### Q2：荣誉信息不显示
**原因**：前端字段名与数据库不匹配

**解决方案**：
确保数据库 honors 表中的字段名为 `title`（不是 `honorName`）

### Q3：轮播不自动启动
**原因**：30 秒计时器可能被中断

**解决方案**：
1. 打开浏览器开发者工具（F12）
2. 查看 Console 是否有错误
3. 检查网络连接是否正常

### Q4：荣誉榜中员工顺序不对
**原因**：中心排序算法的预期行为

**解决方案**：
这是正常的。中心排序会优先展示中间位置的员工，以确保重要员工不会被遗漏。

---

## 📈 性能优化建议

1. **数据缓存**：荣誉数据较少变化，可以缓存在前端
2. **图片优化**：确保员工照片大小合理，避免加载过慢
3. **批量加载**：使用分页加载，避免一次加载过多数据
4. **定时清理**：定期清理过期的荣誉记录

---

## 🔐 安全建议

1. **权限控制**：只有管理员可以添加/编辑荣誉记录
2. **数据验证**：在后端验证荣誉数据的合法性
3. **审计日志**：记录所有荣誉数据的修改操作
4. **备份机制**：定期备份荣誉数据

---

## 📞 技术支持

如遇到问题，请检查以下内容：

1. **数据库连接**：确保数据库正常连接
2. **API 响应**：检查后端 API 是否正常返回数据
3. **前端日志**：查看浏览器 Console 是否有错误
4. **网络状态**：确保网络连接正常

---

## 📝 更新日志

### v1.0.0（当前版本）
- ✅ 荣誉榜筛选功能
- ✅ 中心排序算法
- ✅ 自动轮播展示
- ✅ 详情卡片显示荣誉

### 计划功能
- 🔄 荣誉排序（按获奖时间、类型等）
- 🔄 荣誉统计（获奖次数排行）
- 🔄 荣誉筛选（按类型、年份等）
- 🔄 荣誉导出（导出为 Excel）

---

**最后更新**：2026-04-01
**维护者**：系统管理员
