# 员工风采展示系统 - 部署清单与验证指南

**文档版本**: v1.0  
**最后更新**: 2026年4月15日  
**用途**: 确保离线部署的完整性和可靠性

---

## 📋 部署前检查清单

### 硬件环境检查

- [ ] **CPU**: 4核以上，主频 2.0GHz 以上
- [ ] **内存**: 至少 8GB，推荐 16GB
- [ ] **存储**: 至少 50GB 可用空间（SSD 优先）
- [ ] **显示器**: 连接到大屏（1920×1080 或更高分辨率）
- [ ] **网络**: 本地网络连接正常

### 软件环境检查

- [ ] **操作系统**: Windows 10/11 或 Linux（Ubuntu 20.04+）
- [ ] **Node.js**: v22.13.0 或更新版本已安装
- [ ] **npm**: v10.0 或更新版本已安装
- [ ] **pnpm**: v9.0 或更新版本已安装
- [ ] **MySQL**: v8.0 或 MariaDB v10.5 已安装
- [ ] **Git**: 已安装（用于代码管理）
- [ ] **浏览器**: Chrome v120+ 或 Edge v120+ 已安装

### 系统配置检查

- [ ] **防火墙**: 允许本地 3000 端口访问
- [ ] **防病毒软件**: 已排除项目目录和 MySQL 目录
- [ ] **用户权限**: 当前用户有文件读写权限
- [ ] **系统路径**: Node.js 和 MySQL 已添加到 PATH 环境变量

---

## 🔧 部署步骤检查清单

### 第1步：环境准备

- [ ] 创建项目目录（如 `C:\apps\employee_showcase_system`）
- [ ] 复制项目文件到目录
- [ ] 验证文件完整性（包含 `package.json`, `drizzle/`, `client/`, `server/` 等）
- [ ] 复制 `.env.example` 为 `.env`
- [ ] 编辑 `.env` 文件，填入正确的配置参数

### 第2步：数据库配置

- [ ] 启动 MySQL 服务
- [ ] 创建数据库 `employee_showcase`
- [ ] 创建数据库用户 `showcase_user`
- [ ] 授予用户对数据库的所有权限
- [ ] 在 `.env` 中设置正确的 `DATABASE_URL`
- [ ] 运行数据库迁移 (`pnpm drizzle-kit migrate`)
- [ ] 验证数据库表已创建

### 第3步：依赖安装

- [ ] 进入项目目录
- [ ] 运行 `pnpm install` 安装依赖
- [ ] 检查是否有错误或警告
- [ ] 验证 `node_modules` 目录已创建

### 第4步：应用构建

- [ ] 运行 `pnpm build` 构建应用
- [ ] 检查构建是否成功（应该显示 "build complete"）
- [ ] 验证 `dist/` 目录已创建

### 第5步：应用启动

- [ ] 运行 `pnpm dev` 启动开发服务器
- [ ] 检查是否有错误信息
- [ ] 验证服务器在 `http://localhost:3000` 运行

### 第6步：功能验证

- [ ] 打开浏览器访问 `http://localhost:3000`
- [ ] 验证首页正常加载
- [ ] 访问 `http://localhost:3000/showcase` 查看大屏展示
- [ ] 访问 `http://localhost:3000/admin` 进入管理系统
- [ ] 测试员工信息管理功能
- [ ] 测试轮播策略配置功能
- [ ] 测试公司风采照片上传功能

---

## ✅ 验证清单

### 系统启动验证

| 检查项 | 验证方法 | 预期结果 | 状态 |
|--------|--------|--------|------|
| **Node.js** | 运行 `node --version` | 显示 v22.13.0+ | ☐ |
| **npm** | 运行 `npm --version` | 显示 v10.0+ | ☐ |
| **pnpm** | 运行 `pnpm --version` | 显示 v9.0+ | ☐ |
| **MySQL** | 运行 `mysql --version` | 显示 v8.0+ | ☐ |
| **数据库连接** | 运行 `mysql -u root -p` | 成功连接 | ☐ |
| **项目依赖** | 检查 `node_modules` | 目录存在 | ☐ |
| **应用构建** | 检查 `dist/` | 目录存在 | ☐ |

### 应用功能验证

| 功能 | 验证方法 | 预期结果 | 状态 |
|------|--------|--------|------|
| **首页加载** | 访问 http://localhost:3000 | 页面正常显示 | ☐ |
| **大屏展示** | 访问 http://localhost:3000/showcase | 页面正常显示 | ☐ |
| **管理系统** | 访问 http://localhost:3000/admin | 页面正常显示 | ☐ |
| **员工管理** | 在管理系统添加员工 | 员工信息保存成功 | ☐ |
| **照片上传** | 上传公司风采照片 | 照片保存成功 | ☐ |
| **轮播展示** | 在大屏查看轮播 | 自动轮播正常工作 | ☐ |
| **部门筛选** | 选择具体部门 | 显示该部门员工 | ☐ |
| **荣誉榜** | 选择荣誉分类 | 显示符合条件的员工 | ☐ |

### 性能验证

| 指标 | 验证方法 | 预期结果 | 状态 |
|------|--------|--------|------|
| **页面加载时间** | 打开浏览器 DevTools，查看 Network 标签 | < 3 秒 | ☐ |
| **API 响应时间** | 查看 Network 标签中的 XHR 请求 | < 500ms | ☐ |
| **内存占用** | 运行 `tasklist` 或 `top` 命令 | < 500MB | ☐ |
| **CPU 占用** | 查看任务管理器或 `top` 命令 | < 20% | ☐ |
| **磁盘空间** | 检查项目目录大小 | < 2GB | ☐ |

---

## 🔍 常见问题快速诊断

### 问题1：应用无法启动

**诊断步骤**：

1. [ ] 检查 Node.js 是否安装：`node --version`
2. [ ] 检查 MySQL 是否运行：`mysql -u root -p`
3. [ ] 检查依赖是否完整：`ls node_modules | wc -l`
4. [ ] 检查 `.env` 文件是否存在：`ls -la .env`
5. [ ] 查看错误日志：`cat .manus-logs/devserver.log`

**解决方案**：
- 如果 Node.js 未安装，从 https://nodejs.org 下载安装
- 如果 MySQL 未运行，启动 MySQL 服务
- 如果依赖不完整，运行 `pnpm install`
- 如果 `.env` 缺失，复制 `.env.example` 并修改

### 问题2：数据库连接失败

**诊断步骤**：

1. [ ] 检查 MySQL 服务状态：`sudo systemctl status mysql`
2. [ ] 测试数据库连接：`mysql -h localhost -u root -p`
3. [ ] 检查 `.env` 中的 `DATABASE_URL`
4. [ ] 检查数据库是否存在：`mysql -u root -p -e "SHOW DATABASES;"`

**解决方案**：
- 启动 MySQL 服务：`sudo systemctl start mysql`
- 创建数据库：`mysql -u root -p -e "CREATE DATABASE employee_showcase;"`
- 更新 `.env` 中的连接字符串

### 问题3：端口被占用

**诊断步骤**：

1. [ ] 检查 3000 端口占用情况：`netstat -ano | findstr :3000` (Windows) 或 `lsof -i :3000` (Linux)
2. [ ] 查找占用进程的 PID
3. [ ] 检查是否是旧的应用进程

**解决方案**：
- 杀死占用进程：`taskkill /PID <PID> /F` (Windows) 或 `kill -9 <PID>` (Linux)
- 或修改 `.env` 中的 `PORT` 为其他端口（如 8080）

### 问题4：前端无法连接后端

**诊断步骤**：

1. [ ] 打开浏览器 DevTools (F12)
2. [ ] 查看 Console 标签中的错误信息
3. [ ] 查看 Network 标签中的请求状态
4. [ ] 检查后端服务是否正在运行

**解决方案**：
- 确保后端服务正在运行：`pnpm dev`
- 检查 API 端点是否正确
- 清除浏览器缓存：Ctrl+Shift+Delete

---

## 📊 部署验证报告模板

```
部署验证报告
============

部署日期: _______________
部署人员: _______________
部署环境: _______________

系统要求检查
-----------
操作系统: _______________
CPU: _______________
内存: _______________
存储: _______________

软件版本检查
-----------
Node.js: _______________
npm: _______________
pnpm: _______________
MySQL: _______________

部署步骤完成情况
-----------
环境准备: ☐ 完成 ☐ 未完成
数据库配置: ☐ 完成 ☐ 未完成
依赖安装: ☐ 完成 ☐ 未完成
应用构建: ☐ 完成 ☐ 未完成
应用启动: ☐ 完成 ☐ 未完成
功能验证: ☐ 完成 ☐ 未完成

功能测试结果
-----------
首页加载: ☐ 正常 ☐ 异常
大屏展示: ☐ 正常 ☐ 异常
管理系统: ☐ 正常 ☐ 异常
员工管理: ☐ 正常 ☐ 异常
照片上传: ☐ 正常 ☐ 异常
轮播展示: ☐ 正常 ☐ 异常

性能测试结果
-----------
页面加载时间: _______________
API 响应时间: _______________
内存占用: _______________
CPU 占用: _______________

备注
----
_______________________________________________
_______________________________________________
_______________________________________________

签名: _______________  日期: _______________
```

---

## 🚀 生产环境部署检查清单

### 前置条件

- [ ] 应用已在开发环境验证无误
- [ ] 所有敏感信息已从代码中移除
- [ ] 环境变量已正确配置
- [ ] 数据库备份已创建

### 部署步骤

- [ ] 构建生产版本：`pnpm build`
- [ ] 安装 PM2：`npm install -g pm2`
- [ ] 启动应用：`./scripts/start-production.sh`
- [ ] 验证应用运行状态：`pm2 status`
- [ ] 设置开机自启：`pm2 startup && pm2 save`
- [ ] 配置日志监控：`pm2 logs employee-showcase`

### 部署后验证

- [ ] 应用在 `http://localhost:3000` 正常运行
- [ ] 所有功能正常工作
- [ ] 性能指标符合预期
- [ ] 错误日志无异常

### 维护计划

- [ ] 每日检查应用状态
- [ ] 每周备份数据库
- [ ] 每月检查日志文件大小
- [ ] 每季度更新依赖包

---

## 📞 技术支持

如遇到问题，请按以下步骤进行：

1. **查看本文档** - 常见问题快速诊断部分
2. **查看日志文件** - `.manus-logs/` 目录
3. **查看浏览器控制台** - F12 打开开发者工具
4. **查看系统日志** - 检查 MySQL 和 Node.js 错误日志

---

**文档版本**: v1.0  
**最后更新**: 2026年4月15日  
**作者**: Manus AI
